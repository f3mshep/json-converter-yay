import React from 'react'

class FormatButtons extends React.Component{
  constructor(props){
    super(props)
    this.state=({
      selectedFormat: "paste"
    });
  }
};

export default FormatButtons;